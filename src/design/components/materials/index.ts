/**
 * Material Components — Layer 1 of the design system.
 * ─────────────────────────────────────────────────────────────
 * These are the surfaces everything else sits on.
 * Each component encapsulates its rendering strategy
 * (fallback now, Skia later) behind a stable interface.
 */

export { VoidSurface } from './VoidSurface';
export { ChromeSurface } from './ChromeSurface';
export { BrushedSteelSurface } from './BrushedSteelSurface';
export { GlassPanel } from './GlassPanel';
export { EmissionGlow } from './EmissionGlow';
